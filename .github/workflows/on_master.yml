name: On push to master

on:
  push:
    branches: [ "master" ]

jobs:
  build:
    uses: ./.github/workflows/build.yml
    permissions:
      contents: read
#  deployment:
#    needs: build
#    uses: ./.github/workflows/deployment.yml
#    permissions:
#      contents: write
#      packages: write
